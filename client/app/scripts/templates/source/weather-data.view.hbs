 {{! handlebars template, will be compiled by the grunt task. The compiled version will be used in the app to improve perfermance}}
<div class="row weather-info">
  {{#each data}}
    <div class="col-xs-6 col-sm-4 col-md-3 col-lg-2 {{#visibleClass @index}}{{/visibleClass}}">
      <div class="container">
        <img src="{{icon}}" class="img-circle" />
        <p>{{date}}</p>
        <p>{{low}} - {{high}} (&deg;C)</p>
        <p>{{desc}}</p>
      </div>
    </div>
  {{/each}}
</div>